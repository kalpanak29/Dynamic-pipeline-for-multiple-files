# Dynamic-pipeline-for-multiple-files

This function:
1. Reads CSV or Excel files
2. Finds messy/bad data rows
3. Cleans the data
4. Saves cleaned data to staging folder
5. Returns list of messy rows for email
